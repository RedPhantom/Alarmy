<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true">

  <variable name="defaultLayout" value="${longdate} ${level} ${logger} ${message:withException=true}" />
  <variable name="eventlogLayout" value="${message}${newline}${exception:format=ToString}" />

  <targets>
    <target name="logfile" xsi:type="File" fileName="file.txt" layout="${defaultLayout}" />
    <target name="AlarmyService" xsi:type="EventLog" layout="${eventlogLayout}" />
  </targets>

  <rules>
    <logger name="*" minlevel="Info" writeTo="logfile" />
    <logger name="*" minlevel="Error" writeTo="AlarmyService" />
  </rules>
</nlog>